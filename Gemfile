# FILE MUST BE CALLED A "Gemfile"

# Tells Heroku when you get gems you want them from the source url above.
source "https://rubygems.org"

# type ruby -v in terminal to find current version.
ruby "2.2.2"

# now you just need to list all the gems you want to use.
# instructs heroku where to get everything for the app.
gem "sinatra"


# run bundle in terminal

#Create a 'Procfile' w/ "web: ruby server.rb -p $PORT"
# write 'heroku create' in terminal
# git add .
# git commit -m "Something"
# git push
# *****you must git init before anything!!!!******

# heroku git:remote -a project
# if you forget to init ^